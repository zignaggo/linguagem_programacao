/**
  * My types
  * @param {string} name - name of someone
  * @param {string} surname - name of someone
  * @param {number} age - name of someone
  */

function concatFullName(name, surname, age) {
    return `${name} ${surname} ${age}`
}

console.log(concatFullName('Jorge', 2, 'jorge'))